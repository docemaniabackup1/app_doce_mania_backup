'use client';

import React from 'react';

const Footer: React.FC = () => {
  return (
    <footer className="fixed bottom-0 left-0 right-0 z-10 p-4 bg-gray-800 flex justify-center items-center border-t border-gray-700">
    </footer>
  );
};

export default Footer;
