'use client';

import React from 'react';

const Footer: React.FC = () => {
  return (
    <footer className="fixed bottom-0 left-0 right-0 z-10 h-1 bg-gradient-to-t from-slate-900 to-transparent">
    </footer>
  );
};

export default Footer;
