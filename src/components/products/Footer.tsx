'use client';

import React from 'react';

const Footer: React.FC = () => {
  return (
    <footer className="fixed bottom-0 left-0 right-0 z-10 h-4 bg-slate-800 border-t border-slate-700">
    </footer>
  );
};

export default Footer;
